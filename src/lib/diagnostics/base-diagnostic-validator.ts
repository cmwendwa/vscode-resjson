import * as vscode from "vscode";

export class BaseDiagnosticsValidation {
    // @ts-ignore
    // tslint:disable-next-line:no-empty
    public static validate(splitDoc: string[], index: number): vscode.Diagnostic[];
}
